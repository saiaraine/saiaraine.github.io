function setup() {
  createCanvas(1215, 1000,);

}

function draw() {

  if (mouseIsPressed) {
    fill(0,100,0);
  } else {
    fill(0,100,0);
  }
  fill(150,20,255)
  ellipse(mouseX, mouseY, 80, 80);
}